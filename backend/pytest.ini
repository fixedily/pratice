[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
pythonpath = tests
python_files = test_*.py
python_functions = test_*

markers =
    slow: 较慢的集成/并发类用例（可选单独筛选）

# pytest-asyncio v1.x 需要显式注册
addopts = -p pytest_asyncio
